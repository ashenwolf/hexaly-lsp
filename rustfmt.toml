# 120 columns, matching the convention used across these repositories. rustfmt defaults to 100,
# which forces single-expression closures and short struct literals onto five lines each.
max_width = 120
edition = "2024"
